# bomi · 项目现状（.ai-context.md）

> 整合方维护。四端对话每次新会话**第一动作读取本文件**确认项目状态。
> 更新时机：阶段任务完成、技术决策落地、风险出现、架构演进。

## 项目概览

- **名称**：bomi
- **定位**：AI 食物拍照识别 + 饮食打卡 + 健康计划推荐
- **目标用户**：注重饮食健康的 C 端用户（小程序 + iOS）+ 运营人员（管理后台）
- **启动日期**：2026-06-29

## 技术栈（已锁定）

| 层 | 技术栈 | 包路径 | 负责对话 |
|---|---|---|---|
| 共享层 | TypeScript | `packages/shared` | 整合方独占 |
| 小程序前端 | Uni-app + Vue3 + TS → mp-weixin | `packages/miniapp` | 对话① |
| 管理后台 | Vue3 + Vite + Element Plus | `packages/admin` | 对话③ |
| 服务端 | NestJS + TS | `packages/server` | 对话② |
| AI 调用层 | TypeScript（VLM 视觉模型） | `packages/ai` | 对话② |
| iOS 客户端 | Swift 5.9 + SwiftUI（iOS 16+） | `packages/ios` | 对话④ |
| 仓库结构 | Monorepo（pnpm workspace） | 根 | 整合方 |

> iOS 为原生 Swift，不接入 pnpm workspace（非 JS 包）；通过 Swift 镜像类型对接 shared 契约。

## 当前阶段

**Stage 0 · 仓库初始化（已完成）**
- [x] Monorepo 骨架（pnpm-workspace）+ packages/shared 完整骨架
- [x] packages/{miniapp,admin,server,ai} 包级占位
- [x] 根记忆文件 + docs/api-contract + docs/prompts（含 iOS prompt）
- [x] git 初始化 + Stage 0 合并 main + push origin/main
- [x] 分支策略 D005 落地

**Stage 1 · 各端 CLI 初始化（进行中）**
- [ ] 对话①：Uni-app CLI 初始化 `packages/miniapp` — Stage 1 已完成待 push
- [ ] 对话③：Vite 初始化 `packages/admin` — Stage 1 已完成待 push
- [ ] 对话②：NestJS CLI 初始化 `packages/server` + ai 层骨架 — Stage 1 已完成待 push
- [ ] 对话④：Xcode 项目初始化 `packages/ios` + Swift 镜像类型 — 待启动
- [ ] 各端接入 shared 契约（前端 workspace 依赖 / iOS 镜像类型）

## 四端进度看板

| 对话 | 角色 | 包 | 当前状态 | 分支 |
|---|---|---|---|---|
| ① 前端 | 微信小程序前端 | miniapp | Stage 1 已完成，待 push | feat/miniapp-stage1 |
| ② 服务端 | 服务端+AI 调用层 | server + ai | Stage 1 已完成，待 push | feat/server-stage1 |
| ③ API | 管理后台前端 | admin | Stage 1 已完成，待 push | feat/admin-stage1 |
| ④ iOS | iOS 原生客户端 | ios | Stage 1 已合并 main | feat/ios-stage1（已并入 main） |

**Stage 1 合并顺序**（D005）：shared → server(含ai) → 前端(miniapp + admin) → ios

## 分支策略（详见 DECISIONS.md D005）

**模型**：特性分支 + 整合方合并。四端只往自己的 `feat/{端}-stageN` 提交，合并到 main 只能整合方做。

**5 道防线**（防乱根本机制）：
1. 包路径物理隔离（miniapp/admin/server/ai/ios 目录不交叉，合并零冲突）
2. shared 整合方独占（唯一高风险文件单点控制；iOS 用 Swift 镜像，镜像结构亦由整合方同步）
3. 整合方是合并唯一仲裁者
4. 顺序合并：shared → server → 前端 → ios
5. Conventional Commits 前缀追溯归属

**分支命名**：四端对话接到 prompt 后第一动作 `git branch -m trae/agent-* feat/{端}-stageN`，禁止用 trae/agent-* 随机名工作。

**四端铁律**：
1. 只在 `feat/{端}-stageN` 提交，禁止碰 main
2. 禁止改 `packages/shared/`（提案给整合方）；iOS 亦禁止擅改 `packages/ios/Bomi/Shared/` 镜像结构
3. 禁止跨端目录
4. 阶段完成向整合方报告，合并由整合方做

## 整合方独占写权限

以下文件/目录**仅整合方可写**，四端对话只读，需变更须提案：
- `packages/shared/**`
- `packages/ios/Bomi/Shared/**` 的结构（iOS 镜像类型字段由整合方同步）
- `.ai-context.md` / `DECISIONS.md` / `.ai-memory.md` / `TECH_DEBT.md`
- `docs/api-contract.md`
- 根 `package.json` / `pnpm-workspace.yaml` / `.gitignore` / `.env.example` / 分支策略

## 跨端协同铁律

1. 接口字段变更：先改 `shared/types` → 同步 server DTO → 同步前端 api → 同步 iOS 镜像，四处同提交
2. 错误码新增：先改 `shared/constants/error-code.ts` → server 使用 → 前端处理 → iOS 镜像
3. 业务状态变更：先改 `shared/constants/business.ts` → server DB/逻辑 → 前端展示 → iOS 镜像
4. 前端（含 iOS）禁直连 AI 供应商，统一经 server `/api/ai/*` 转发
5. API Key 仅 `packages/ai/config/env.ts` 从 `process.env` 读取，禁止硬编码、禁止入 shared、禁止入 iOS 工程

## iOS 专属约定

- **登录**：Apple Sign In（App Store 强制）+ 微信 SDK + 手机号验证码，三选一
- **shared 对接**：Swift 镜像类型方案，逐字段对应 TS，镜像文件头标注来源，整合方同步
- **token 存储**：Keychain（非 UserDefaults）
- **微信 AppID**：iOS 需微信开放平台 iOS AppID（非小程序 AppID）

# bomi · 项目现状（.ai-context.md）

> 整合方维护。四端对话每次新会话**第一动作读取本文件**确认项目状态。
> 更新时机：阶段任务完成、技术决策落地、风险出现、架构演进。

## 项目概览

- **名称**：bomi
- **定位**：AI 食物拍照识别 + 饮食打卡 + 健康计划推荐
- **目标用户**：注重饮食健康的 C 端用户（小程序 + iOS）+ 运营人员（管理后台）
- **启动日期**：2026-06-29

## 技术栈（D008 架构转向后）

| 层 | 技术栈 | 路径 | 负责对话 |
|---|---|---|---|
| 共享层（契约） | **protobuf**（单一来源 + 多语言 codegen） | `proto/` | 整合方独占 |
| 服务端 | **Go**（Gin/Echo + GORM） | `server/` | 服务端对话 |
| AI 调用层 | **Go**（内置在 server/internal/ai/） | `server/internal/ai/` | 服务端对话 |
| iOS | **KMP 共享逻辑 + SwiftUI** | `ios/` + `mobile-shared/` | iOS 对话 |
| Android | **KMP 共享逻辑 + Jetpack Compose** | `android/` + `mobile-shared/` | Android 对话（新增） |
| 管理后台 | Vue3 + Vite + Element Plus | `packages/admin/` | 管理后台对话 |
| 小程序 | Uni-app + Vue3 → mp-weixin | `packages/miniapp/` | 小程序对话（搁置） |
| 移动端共享 | **KMP**（Kotlin Multiplatform） | `mobile-shared/` | 整合方协调 |
| Monorepo | **混合**：pnpm（前端）+ Go modules + Gradle（KMP） | 根 Makefile | 整合方 |

> D001/D006/D007 的 NestJS/Swift 镜像/TS shared 方案已被 D008 覆盖废弃。

## 当前阶段

**Stage 0.5 · 架构迁移（进行中，D008）**
- [ ] proto/ 目录初始化 + buf 配置 + 首批 proto 定义
- [ ] server/ Go 项目初始化
- [ ] mobile-shared/ KMP 模块初始化
- [ ] iOS Stage 1 迁移（Swift 镜像 → KMP 集成）
- [ ] Android Stage 1 初始化
- [ ] admin 类型来源从 @bomi/shared 改为 gen/ts/
- [ ] 旧 packages/shared + packages/server + packages/ai 清理
- [ ] skill 文档 + prompt 文档重写

**已完成**
- [x] Stage 0 · 仓库初始化（Monorepo 骨架）
- [x] D008 架构决策落地

## 五端进度看板（D008 后）

| 对话 | 端 | 当前状态 | 分支 |
|---|---|---|---|
| 整合方 | proto + 协调 | D008 决策落地，待 proto 初始化 | main |
| 服务端 | Go server + ai | 待 Go 项目初始化（原 NestJS 废弃） | - |
| iOS | KMP + SwiftUI | Stage 1 需迁移（Swift 镜像 → KMP） | - |
| Android | KMP + Compose | 待启动（新增端） | - |
| 管理后台 | Vue3 admin | 待 push（类型来源待改 gen/ts/） | - |
| 小程序 | Uni-app | 搁置 | - |

## 分支策略（D005 仍然有效，D008 后端名更新）

**模型**：特性分支 + 整合方合并。五端只往自己的 `feat/{端}-stageN` 提交，合并到 main 只能整合方做。

**分支命名**：`feat/server-stageN` / `feat/ios-stageN` / `feat/android-stageN` / `feat/admin-stageN` / `feat/mobile-shared-stageN`

**合并顺序**（D008 后）：proto → server(Go) → mobile-shared(KMP) → ios/android → admin/miniapp

**铁律**：
1. 只在 `feat/{端}-stageN` 提交，禁止碰 main
2. 禁止改 `proto/`（提案给整合方）；mobile-shared 结构由整合方协调
3. 禁止跨端目录
4. 完成后立即 commit + push（吸取代码丢失教训）
5. Conventional Commits 前缀：`feat(server):` / `feat(ios):` / `feat(android):` / `feat(admin):` / `feat(kmp):`

## 整合方独占写权限

以下文件/目录**仅整合方可写**，五端对话只读，需变更须提案：
- `proto/**`（protobuf 单一来源）
- `buf.yaml` / `buf.gen.yaml`（codegen 配置）
- `mobile-shared/` 的结构（KMP 模块结构由整合方协调）
- `.ai-context.md` / `DECISIONS.md` / `.ai-memory.md` / `TECH_DEBT.md`
- `docs/api-contract.md`
- 根 `Makefile` / `pnpm-workspace.yaml` / `.gitignore` / `.env.example`

## 跨端协同铁律（D008 后）

1. 接口字段变更：先改 `proto/` → `buf generate` → 各端同步生成代码
2. 错误码新增：先改 `proto/bomi/enum/error_code.proto` → codegen → 各端使用
3. 前端（含 iOS/Android）禁直连 AI 供应商，统一经 server `/api/ai/*` 转发
4. API Key 仅 `server/internal/ai/config/` 从环境变量读取，禁止硬编码、禁止入 proto、禁止入移动端
5. iOS/Android 业务逻辑走 KMP 共享层（mobile-shared/），UI 各端原生

## 移动端专属约定（D008 后）

- **iOS**：KMP 共享逻辑 + SwiftUI（UI 原生）；Apple Sign In + 微信 SDK + 手机号
- **Android**：KMP 共享逻辑 + Jetpack Compose（UI 原生）；Google Sign In + 微信 SDK + 手机号
- **token 存储**：iOS Keychain / Android EncryptedSharedPreferences
- **KMP 共享**：网络层、数据层、用例层在 mobile-shared/src/commonMain/；iOS/Android 各自实现 UI

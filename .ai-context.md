# bomi · 项目现状（.ai-context.md）

> 整合方维护。五端对话每次新会话**第一动作读取本文件**确认项目状态。
> 更新时机：阶段任务完成、技术决策落地、风险出现、架构演进。

## 项目概览

- **名称**：bomi
- **定位**：AI 食物拍照识别 + 饮食打卡 + 健康计划推荐
- **目标用户**：注重饮食健康的 C 端用户（iOS + Android）+ 运营人员（管理后台）
- **启动日期**：2026-06-29
- **核心隐私承诺**：用户饮食明细、照片等隐私数据完全本地化，不上传后端（D011）

## 技术栈（D008 架构 + D009-D013 隐私修订）

| 层 | 技术栈 | 路径 | 负责对话 |
|---|---|---|---|
| 共享层（契约） | **protobuf**（单一来源 + 多语言 codegen） | `proto/` | 整合方独占 |
| 服务端 | **Go**（Gin + GORM + pgx） | `server/` | 服务端对话 |
| AI 调用层 | **Go**（内置在 server/internal/ai/） | `server/internal/ai/` | 服务端对话 |
| 对象存储 | **腾讯云 COS**（双桶，无 CDN） | server/internal/storage/ | 服务端对话 |
| iOS | **KMP + SwiftUI + CloudKit** | `ios/` + `mobile-shared/` | iOS 对话 |
| Android | **KMP + Compose + 坚果云WebDAV** | `android/` + `mobile-shared/` | Android 对话 |
| 管理后台 | Vue3 + Vite + Element Plus | `packages/admin/` | 管理后台对话 |
| 小程序 | Uni-app + Vue3 → mp-weixin | `packages/miniapp/` | 小程序对话（搁置） |
| 移动端共享 | **KMP**（Kotlin Multiplatform） | `mobile-shared/` | 整合方协调 |
| 本地数据库 | **SQLDelight**（封装 SQLite，存隐私数据） | mobile-shared/ | 整合方协调 |
| Monorepo | **混合**：pnpm + Go modules + Gradle | 根 Makefile | 整合方 |

> D001/D006/D007 的 NestJS/Swift 镜像/TS shared 方案已被 D008 覆盖废弃。
> D009-D013 落地后，后端无饮食打卡接口，饮食数据完全本地化。

## 数据分层（D009-D013 核心）

| 数据类型 | 存储位置 | 是否上传后端 |
|---|---|---|
| 用户饮食明细（食物/照片/体重） | SQLDelight 本地 + 私有云（iCloud/坚果云） | ❌ 禁止上传 |
| AI 识别临时图片 | COS 临时桶（5分钟自动清理） | 临时上传，用完即删 |
| 会员/积分/邀请/内购 | 后端 PostgreSQL | ✅ 正常存储 |
| 运营素材（海报/主题） | COS 永久桶 | ✅ 正常存储 |

## 当前阶段

**Stage 0.5 · D009-D013 隐私架构修订（进行中）**
- [x] proto 修订（删 DietService + 加 DeleteRecognizeImage + RecentNutritionSummary）
- [x] server 修订（pgx + COS 双桶 + storage/cos.go + 删 diet 路由）
- [x] mobile-shared 修订（LocalDietStorage + CloudSync + ImageUploader 三个 expect 抽象）
- [x] DECISIONS D009-D013 写入
- [ ] docs/skills/ + docs/prompts/ 重写（反映 D009-D013）
- [ ] docs/api-contract.md 更新
- [ ] 提交 push + 分发各端 Stage 1 任务卡

**已完成**
- [x] Stage 0.5 · D008 架构迁移骨架（proto 15 + server 14 + mobile-shared 16）
- [x] Stage 0 · 仓库初始化
- [x] D008 架构决策落地

## 五端进度看板（D009-D013 后）

| 对话 | 端 | 当前状态 | 分支 |
|---|---|---|---|
| 整合方 | proto + 协调 | D009-D013 修订中，待 push | main |
| 服务端 | Go server + ai + COS | 待 Stage 1（PG + COS 双桶 + AI handler） | - |
| iOS | KMP + SwiftUI + CloudKit | 待 Stage 1（KMP 集成 + 本地存储） | - |
| Android | KMP + Compose + 坚果云 | 待 Stage 1（新建项目 + 本地存储） | - |
| 管理后台 | Vue3 admin | 待 Stage 1（类型来源改 gen/ts/） | - |
| 小程序 | Uni-app | 搁置 | - |

## 分支策略（D005 + D008）

**模型**：特性分支 + 整合方合并。五端只往自己的 `feat/{端}-stageN` 提交，合并到 main 只能整合方做。

**分支命名**：`feat/server-stageN` / `feat/ios-stageN` / `feat/android-stageN` / `feat/admin-stageN` / `feat/mobile-shared-stageN`

**合并顺序**：proto → server(Go) → mobile-shared(KMP) → ios/android → admin/miniapp

**铁律**：
1. 只在 `feat/{端}-stageN` 提交，禁止碰 main
2. 禁止改 `proto/`（提案给整合方）；mobile-shared 结构由整合方协调
3. 禁止跨端目录
4. 完成后立即 commit + push（吸取代码丢失教训）
5. Conventional Commits 前缀：`feat(server):` / `feat(ios):` / `feat(android):` / `feat(admin):` / `feat(kmp):`

## 整合方独占写权限

以下文件/目录**仅整合方可写**，五端对话只读，需变更须提案：
- `proto/**`（protobuf 单一来源）
- `buf.yaml` / `buf.gen.yaml`（codegen 配置）
- `mobile-shared/` 的结构（KMP 模块结构由整合方协调）
- `.ai-context.md` / `DECISIONS.md` / `.ai-memory.md` / `TECH_DEBT.md`
- `docs/api-contract.md`
- 根 `Makefile` / `pnpm-workspace.yaml` / `.gitignore` / `.env.example`

## 跨端协同铁律（D008 + D009-D013）

1. 接口字段变更：先改 `proto/` → `buf generate` → 各端同步生成代码
2. 错误码新增：先改 `proto/bomi/enum/error_code.proto` → codegen → 各端使用
3. 前端（含 iOS/Android）禁直连 AI 供应商，统一经 server `/api/ai/*` 转发
4. API Key 仅 `server/internal/ai/` 从环境变量读取，禁止硬编码、禁止入 proto、禁止入移动端
5. iOS/Android 业务逻辑走 KMP 共享层（mobile-shared/），UI 各端原生
6. **D011**：饮食打卡明细完全本地化，后端无 diet 接口，App 用 LocalDietStorage + CloudSync
7. **D010**：AI 识别图片临时上传 COS，用户确认后删除，5分钟生命周期兜底
8. **D012**：开发期用 Neon PG，上线切换自建 PG，改连接串即可

## 移动端专属约定（D008 + D009-D013）

- **iOS**：KMP + SwiftUI；Apple Sign In + 微信 SDK + 手机号；CloudKit 私有同步；Keychain
- **Android**：KMP + Compose；Google Sign In + 微信 SDK + 手机号；坚果云 WebDAV 同步；KeyStore + DataStore
- **本地存储**：SQLDelight 封装 SQLite，存饮食打卡明细（D011）
- **私有云同步**：iOS iCloud CloudKit / Android 坚果云 WebDAV，跨设备同步本地数据（D011）
- **图片上传**：各端 ImageUploader actual 实现，压缩+传 COS 临时桶（D010）
- **KMP 共享**：网络层、数据层、用例层在 mobile-shared/src/commonMain/；iOS/Android 各自实现 UI

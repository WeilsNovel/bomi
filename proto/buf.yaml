version: v2
modules:
  - path: .
    name: buf.build/bomi/bomi
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
    - FILE

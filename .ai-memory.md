# bomi · 进行中任务（.ai-memory.md）

> 整合方维护。记录当前进行中的任务与上下文，五端阶段任务完成后提示整合方更新。

## 当前进行中

### [整合方] Stage 0.5 · D008 架构迁移（骨架完成，待提交 push）
- **状态**：proto/ + server/ + mobile-shared/ 骨架已创建，server/ 通过 `go build ./...`，待 commit + push
- **git 现状**：工作区有大量新增文件（proto 15 + server 14 + mobile-shared 16 + Makefile + .gitignore + 清理旧包）
- **下一步**：
  1. ✅ 整合方初始化 proto/ 目录 + buf 配置 + 首批 proto 定义（15 文件）
  2. ✅ 整合方初始化 server/ Go 项目骨架（14 文件，go build 通过）
  3. ✅ 整合方初始化 mobile-shared/ KMP 模块骨架（16 文件）
  4. ✅ 清理旧的 packages/shared + packages/server + packages/ai
  5. ✅ Makefile + .gitignore 更新
  6. ⬜ commit + push（当前最高优先级，防止沙箱销毁丢代码）
  7. ⬜ 重写 skill 文档 + prompt 文档（技术栈全变）
  8. ⬜ 分发各端 Stage 1 任务卡（新技术栈）

### 迁移影响（已评估，见 DECISIONS.md D008）
- packages/shared（TS）→ 废弃，已删除，迁移到 proto/
- packages/server（NestJS）→ 废弃，已删除，改为 server/（Go）
- packages/ai（TS 包）→ 废弃，已删除，合并到 server/internal/ai/（Go）
- iOS Stage 1（Swift 镜像）→ 部分重构，Shared/ 改为 KMP 集成，SwiftUI View 保留
- D006/D007 → 废弃，D008 覆盖
- docs/prompts/ + docs/skills/ → 需重写

## 已完成

### [整合方] Stage 0.5 架构迁移骨架（2026-07-10）
- proto/：15 文件（buf 配置 + enum/model/api 完整定义）
- server/：14 文件（go.mod + cmd/main + config(2) + constants + middleware(4) + handler + ai(4) + router + .env.example + README），`go build ./...` 通过
- mobile-shared/：16 文件（build.gradle.kts + settings + commonMain(8) + androidMain + iosMain + README）
- Makefile：proto/build/test/clean 编排
- .gitignore：新增 Go/Gradle/gen 产物忽略
- 清理：删除 packages/shared + packages/server + packages/ai，更新 root/admin/miniapp package.json

### [整合方] D008 架构转向决策（2026-06-30）
- 服务端 NestJS(TS) → Go
- iOS 纯 Swift → KMP + SwiftUI
- 新增 Android（KMP + Compose）
- shared TS 单一来源 → protobuf 单一来源 + codegen
- AI 层 TS 包 → Go 内置
- Monorepo pnpm → 混合（pnpm + Go modules + Gradle）
- DECISIONS.md D008 落地，.ai-context.md 技术栈更新

### [整合方] iOS Stage 1 审查 + 合并 main（2026-07-06）
- Swift 镜像方案已完成并合并（D008 后需迁移为 KMP 集成）

### [整合方] Stage 0 仓库初始化（2026-06-29）
- Monorepo 骨架 + shared + 文档 + git 初始化

## 待办任务

### 各端 Stage 1 任务卡（迁移骨架 push 后分发）
- 服务端：在 server/ 骨架基础上实现 auth/food/plan/ai handler + GORM 接入
- iOS：KMP 集成（引入 mobile-shared framework）+ SwiftUI View（迁移已有 Stage 1 成果）
- Android：新建项目，KMP 集成 + Compose UI
- 管理后台：类型来源从 @bomi/shared 改为 gen/ts/（proto codegen）
- mobile-shared：KMP commonMain 已就绪，各端接入后联调

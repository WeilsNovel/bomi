# bomi · 进行中任务（.ai-memory.md）

> 整合方维护。记录当前进行中的任务与上下文，五端阶段任务完成后提示整合方更新。

## 当前进行中

### [整合方] D009-D013 隐私架构修订 + Stage 0.5 收尾（进行中）
- **状态**：proto + server + mobile-shared 已按 D009-D013 修订完成，待 push
- **变更**：
  - D009 PG + pgx + GORM
  - D010 AI 图片临时上传-用户确认后删除-5分钟兜底清理
  - D011 饮食打卡完全本地化（删 DietService + 加 LocalDietStorage/CloudSync）
  - D012 Neon 开发期 + 自建 PG 上线
  - D013 双 COS 桶 + 无 CDN
- **待办**：
  1. 更新 docs/skills/ + docs/prompts/ 各端文档（反映 D009-D013）
  2. 更新 docs/api-contract.md
  3. 提交 push
  4. 分发各端 Stage 1 任务卡

### 待分发：各端 Stage 1 任务卡（D009-D013 修订后）
- 服务端：在 server/ 骨架基础上实现 auth/food/plan/ai handler + GORM(PG) + COS 双桶
- iOS：KMP 集成 + SwiftUI + CloudKit 私有同步 + Keychain
- Android：KMP 集成 + Compose + 坚果云WebDAV + KeyStore
- 管理后台：类型来源从 @bomi/shared 改为 gen/ts/（proto codegen）
- mobile-shared：commonMain 已就绪，各端接入后联调

## 架构关键变化（D009-D013）

### 后端不再有饮食打卡接口
- proto 删除 DietService 全部定义
- 服务端 router 无 diet 路由
- 后端只存：会员/积分/邀请/内购/素材URL

### AI 识别新流程
1. App 压缩图 → 上传 COS 临时桶 → 得 imageKey
2. App 调 recognize(imageKey) → 后端调 VLM → 返回文字营养数据 + imageKey
3. 用户确认 → App 调 deleteImage(imageKey) → 后端删 COS 原图
4. 兜底：5分钟生命周期自动清理

### 本地存储 + 私有云同步
- mobile-shared 新增 3 个 expect 抽象：
  - LocalDietStorage（SQLDelight）
  - CloudSync（iOS CloudKit / Android 坚果云）
  - ImageUploader（压缩+传COS临时桶）
- BomiSDK.create() 注入 4 个依赖

## 已完成

### [整合方] Stage 0.5 架构迁移骨架（2026-07-10）
- proto/：15 文件（D009-D013 修订后：删 DietService + 加 DeleteRecognizeImage + RecentNutritionSummary）
- server/：16 文件（+ storage/cos.go 双桶 + COSConfig + pgx/GORM PG driver）
- mobile-shared/：19 文件（+ LocalDietStorage + CloudSync + ImageUploader）
- Makefile + .gitignore + skills/prompts

### [整合方] D008 架构转向决策（2026-06-30）
- 服务端 NestJS(TS) → Go
- iOS 纯 Swift → KMP + SwiftUI
- 新增 Android（KMP + Compose）
- shared TS 单一来源 → protobuf 单一来源 + codegen

### [整合方] iOS Stage 1 审查 + 合并 main（2026-07-06）
- Swift 镜像方案已完成并合并（D008 后需迁移为 KMP 集成）

### [整合方] Stage 0 仓库初始化（2026-06-29）
- Monorepo 骨架 + shared + 文档 + git 初始化

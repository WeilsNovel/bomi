# bomi · 进行中任务（.ai-memory.md）

> 整合方维护。记录当前进行中的任务与上下文，四端阶段任务完成后提示整合方更新。
> 完成的任务移到「已完成」段落，保留简短摘要。

## 当前进行中

### [整合方] Stage 1 收尾 · iOS 已合并，等待三端 push
- **状态**：iOS Stage 1 审查通过并合并 main（`7593eaf`）；三端 miniapp/server/admin 仍待 push
- **git 现状**：origin/main 在 `7593eaf`（含 iOS 完整工程 28 文件）
- **下一步**：用户让三端 push feat 分支 → 整合方 fetch 审查 → 按序合并 main

### 注意事项
- iOS 对话在 `trae/agent-57IsVC` 分支提交（环境自动建分支，未重命名为 feat/ios-stage1），整合方已用 `feat/ios-stage1` 分支名合并
- iOS 对话多打了 archive 压缩包与对话记录，整合方合并时已清理（commit `7593eaf` 删除 31 文件）
- 三端 push 后审查要点：分支是否重命名为 feat/{端}-stage1、是否动 shared、DTO 对齐、AI Key 走 env

## 待办任务

### 三端 push 后的审查清单
1. fetch 三端 feat 分支
2. 按序审查：server（含 ai）→ miniapp → admin
3. 审查要点：
   - server：DTO 对齐 shared、AI Key 走 process.env、响应拦截器输出 BaseApiResponse、JWT Guard
   - miniapp/admin：接口对齐 api-contract、引用 @bomi/shared、未碰他端目录
   - 三端是否动过 packages/shared/（应为否）
   - 分支命名是否 feat/{端}-stage1
4. 按序合并：shared → server → 前端(miniapp+admin)
5. 三端全合并后分发 Stage 2 任务卡

## 已完成

### [整合方] iOS Stage 1 审查 + 合并 main（2026-07-06）
- 审查通过：镜像类型逐字段对齐（AppleLoginRequest 5 字段含嵌套 AppleFullName、错误码 40114 已镜像）
- APIClient 解包 BaseApiResponse<T>，code≠0 抛 BomiError，token 从 Keychain 读
- AppConfig.useAiProxy = true，无任何 AI API Key
- shared 零改动（diff 空）
- 清理冗余 archive 压缩包与对话记录（31 文件删除）
- 合并 commit `7593eaf` 到 main 并 push origin

### [整合方] iOS 端新增 D006（2026-06-29）
- shared/types/user.ts 新增 AppleLoginRequest
- shared/constants/error-code.ts 新增 APPLE_LOGIN_FAILED: 40114 + 文案
- docs/api-contract.md 新增 POST /api/auth/apple-login
- docs/prompts/ios.md 新增对话④ iOS prompt
- DECISIONS.md 落地 D005 分支策略 + D006 iOS 端决策
- .ai-context.md 升级四端看板

### [整合方] Stage 0 仓库初始化 + 合并 main（2026-06-29）
- Monorepo 骨架 + packages/shared 完整骨架
- 根记忆文件 + docs/api-contract + docs/prompts
- git 仓库级身份配置（WeilsNovel）
- Stage 0 提交合并 main 并 push origin/main
- 分支策略 D005 落地
